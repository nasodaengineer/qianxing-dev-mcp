# Research archive note

This MCP repo includes the deep-research **markdown** corpus, summaries, drafts, `nodes/node-index.json`, and Miyoushe index/stubs.

**Not vendored here** (live in the local research pack `/workspace/qianxing-deep/`):

- `raw-html/` — full fetched HTML pages (~5.9MB)
- `nodes/node-pages.json`, Miyoushe `raw-*.json` probes
- `catalog-raw.json`, `fetch-results.json`

Use those archives when you need raw page HTML or full fetch telemetry.
